import type { Player, Team } from "@/lib/types";
export function rosterCsv(teams: Team[], players: Player[]) { const rows = [["Team", "Player", "Skill", "Sold Price", "Status"]]; for (const team of teams) for (const player of players.filter((item) => item.teamId === team.id)) rows.push([team.name, player.name, player.primarySkill, String(player.soldPrice ?? ""), player.status]); for (const player of players.filter((item) => item.status === "unsold")) rows.push(["UNSOLD", player.name, player.primarySkill, "", player.status]); return rows.map((row) => row.map((cell) => `"${String(cell).replaceAll('"', '""')}"`).join(",")).join("\n"); }
