import type { Team } from "@/lib/types";
import { AUCTION_RULES } from "@/lib/auction";
import { formatINR } from "@/lib/utils";
export function PurseTicker({ teams }: { teams: Team[] }) { return <div className="glass rounded-[2rem] p-5"><h2 className="mb-4 text-lg font-bold">Live purse ticker</h2><div className="grid gap-4 md:grid-cols-2 xl:grid-cols-4">{teams.map((team) => <article key={team.id} className="rounded-2xl border border-white/10 bg-white/[.03] p-4"><div className="mb-3 flex items-center justify-between"><span className="font-bold">{team.name}</span><span className="rounded-full bg-white/10 px-2 py-1 text-xs">{team.playersBought} players</span></div><div className="h-3 rounded-full bg-white/10"><div className="h-full rounded-full" style={{ width: `${(team.purseRemaining / AUCTION_RULES.teamPurse) * 100}%`, background: `linear-gradient(90deg, ${team.primaryColor}, ${team.secondaryColor})` }} /></div><p className="mt-2 text-sm text-slate-300">{formatINR(team.purseRemaining)} remaining</p></article>)}</div></div>; }
