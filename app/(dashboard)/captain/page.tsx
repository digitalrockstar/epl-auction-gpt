import { Shell } from "@/components/layout/shell";
import { getAuctionSnapshot } from "@/lib/auction-store";
export default async function CaptainPage() { const { players } = await getAuctionSnapshot(); const captainPool = players.filter((player) => player.wantsCaptain); return <Shell><section className="glass rounded-[2rem] p-6"><p className="text-sm uppercase tracking-[.25em] text-cyan-200/70">Captain console</p><h1 className="mt-2 text-4xl font-black">Interested captains</h1><div className="mt-6 grid gap-3 md:grid-cols-2">{captainPool.map((player) => <article key={player.id} className="rounded-2xl bg-white/[.04] p-4"><h2 className="text-xl font-bold">{player.name}</h2><p className="text-slate-300">{player.role} • {player.primarySkill}</p></article>)}</div></section></Shell>; }
